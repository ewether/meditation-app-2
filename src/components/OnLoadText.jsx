import { render } from '@testing-library/react';
import React from 'react';
import '../App.css';

function OnLoadText() {
    return (
        <h2 className="on-load-text">Let's Relax</h2>
    );
};

export default OnLoadText;