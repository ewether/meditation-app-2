import { render } from '@testing-library/react';
import React from 'react';
import '../App.css';

function LoadText() {
    return (
        <h2 className="on-load-text">Let's Relax</h2>
    );
};

export default LoadText;