import React from "react";
import "../App.css";

function BeginButton({onBegin, text}) {
  return (
      null
    // <button className="begin-button" onClick={onBegin}>{text}</button>
    // button that triggers next screen
  );
}

export default BeginButton;